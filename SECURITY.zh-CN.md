# 安全策略

[English](SECURITY.md)

## 支持版本

| 版本 | 支持状态 |
| --- | --- |
| `main` | 支持 |
| 未发布的 Draft 快照 | 不保证稳定性 |

## 报告漏洞

仓库可用时，请使用 GitHub 私密漏洞报告。若该私密渠道不可用，不要在公开 issue 中加入漏洞细节；请通过已经建立的私密渠道联系 Tinkora 组织所有者。在公开受监控的安全联系方式前，不承诺响应时限。

报告应包含受影响 revision、影响、最小复现和建议的缓解方式。不要包含真实凭据或私有用户数据。

## 信任边界

- 应用只在浏览器标签页内存中处理输入，没有上传、账户、持久化、Worker 或 MCP transport。
- 输入必须是最大 10 MiB 的有效 UTF-8。
- 表格单元格通过 DOM `textContent` 渲染，不使用输入构造 HTML。
- CSV 和 TSV 导出会保留输入值，不会中和公式样式的前缀；在电子表格软件中打开不可信输出属于独立信任边界。
- SQL 导出只生成供检查的文本，不会连接数据库或执行语句。

增加托管、身份验证、持久化或 Agent transport 时，必须重新定义并测试对应的信任边界。
